#include "main.h"
#include <stdio.h>
/**
* add - adds two integers and gives result
* @a: first integer being added
* @b: second integer being added
* Return: Always 0 (success)
*/
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
